import "./Header.scss";

const Header = () => {
  return (
    <header>
      <h1>Fun with AI </h1>
      <span>
        GPT-3 is a powerful AI model created by OpenAI. It can process plain
        text prompts and produce outputs that are hard to distinguish from human
        writing.
      </span>
    </header>
  );
};

export default Header;
